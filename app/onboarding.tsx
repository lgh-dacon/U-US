/**
 * 역할: 온보딩 화면 컴포넌트를 Expo Router 경로에 연결하는 라우트 파일입니다.
 */
import { OnboardingScreen } from '../src/screens/OnboardingScreen';

export default function OnboardingRoute() {
  return <OnboardingScreen />;
}
